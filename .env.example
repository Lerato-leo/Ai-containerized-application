# AI Finance Application - Environment Configuration Example
# Copy this file to .env and update with your actual values

# ==============================================================================
# APPLICATION SETTINGS
# ==============================================================================

# Backend server port (default: 3000)
PORT=3000

# Node environment (development, production, testing)
NODE_ENV=production

# ==============================================================================
# AI/LLM CONFIGURATION (Google Gemini)
# ==============================================================================

# AI Provider: gemini (primary provider used in this application)
AI_PROVIDER=gemini

# Google Gemini API Key - REQUIRED for AI features
# Get from: https://ai.google.dev/tutorials/setup
AI_API_KEY=your_gemini_api_key_here

# AI Model to use with Gemini
# Options: 
#   - gemini-1.5-flash (fast, efficient, lower cost - recommended)
#   - gemini-1.5-pro (more capable, higher latency and cost)
AI_MODEL=gemini-1.5-flash

# ==============================================================================
# DATABASE CONFIGURATION
# ==============================================================================

# Data directory for JSON files (default: ./data)
# For Kubernetes: Use persistent volume mount
DATA_DIR=./data

# ==============================================================================
# FRONTEND CONFIGURATION
# ==============================================================================

# Frontend port (if running separately from backend)
FRONTEND_PORT=3001

# ==============================================================================
# KUBERNETES / DOCKER CONFIGURATION
# ==============================================================================

# Kubernetes namespace name
K8S_NAMESPACE=ai-finance

# Docker image tags
DOCKER_BACKEND_IMAGE=leratomatamela1/ai-finance-backend:latest
DOCKER_FRONTEND_IMAGE=leratomatamela1/ai-finance-frontend:latest

# ==============================================================================
# LOGGING & DEBUG
# ==============================================================================

# Log level (error, warn, info, debug)
LOG_LEVEL=info

# Enable debug mode (true or false)
DEBUG=false

# ==============================================================================
# NOTES FOR DEVELOPERS
# ==============================================================================
# 1. NEVER commit the actual .env file with real API keys to Git
# 2. Always use .env.example as the template for required variables
# 3. For local development: Create .env file and populate with test values
# 4. For Docker: Pass environment variables via:
#    - docker run -e KEY=value
#    - docker-compose environment section
#    - Kubernetes secrets and environment variables
# 5. For production: Use secure secrets management (AWS Secrets Manager, Azure Key Vault, etc.)
